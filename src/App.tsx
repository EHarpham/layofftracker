import { LayoffWidget } from './components/LayoffWidget'

function App() {
  return (
    <main className="flex min-h-screen items-center justify-center p-6" style={{ background: 'var(--c-bg-page)' }}>
      <LayoffWidget />
    </main>
  )
}

export default App
